/* eslint-disable react/prop-types */
import logo from '../../Images/wellness-logo.jpeg';

function Logo({className}) {
    return <img src={logo} alt="Logo containing a wave and leave style drawing" className={className} />
}

export default Logo;